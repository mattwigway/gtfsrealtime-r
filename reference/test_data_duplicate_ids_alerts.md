# Write an alerts feed with duplicate IDs the first one is "id" and will not be expanded the second one is also id and has two TimeRanges so will be expanded

Write an alerts feed with duplicate IDs the first one is "id" and will
not be expanded the second one is also id and has two TimeRanges so will
be expanded

## Usage

``` r
test_data_duplicate_ids_alerts(filename)
```

## Arguments

- filename:

  filename to write feed to
