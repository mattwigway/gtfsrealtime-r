# Make sure we can read all values in vehicle positions For the other types this is tested incidentally by the unwrapping tests, but there's no unwrapping test for positions since each vehicle position record becomes just one row.

Make sure we can read all values in vehicle positions For the other
types this is tested incidentally by the unwrapping tests, but there's
no unwrapping test for positions since each vehicle position record
becomes just one row.

## Usage

``` r
test_data_positions_all_values(filename)
```
